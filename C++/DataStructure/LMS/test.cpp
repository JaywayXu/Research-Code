#include <iostream>
#include "LMS.h"
#include "Book.h"
#include "BTree.h"

using namespace std;

int main() {
    // Book a(1,"1","1",1,1);
    // Book b(1,"2","1",1,1);
    // Book c(3,"3","3",3,3);
    // BTree<Book> *ptr = new BTree<Book>();

    // ptr->insert(a);
    // ptr->display();
    
    // ptr->insert(b);
    // ptr->display();

    // ptr->insert(c);
    // ptr->display();

    // int x;
    // while (cin>>x && x!=-1) {
    //     ptr->insert(x);
    //     ptr->display();
    // }
    // int y;
    // while (cin>>y && y!=-1) {
    //     ptr->remove(y);
    //     ptr->display();
    // }
    return 0;
}